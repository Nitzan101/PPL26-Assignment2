Q1.1: Yes. Contradictory example:
(define x 10)
(define y (* x 2))
# Without hard-coding like placing 10 everywhere x is, you can't create the same functionality.

Q1.2: Yes. Contradictory example:
    (define f
        (lambda (x)
            (if (< x 10) x (f (/ x 10)))
        )
    )
    (f 123)

# Without 'define' we can't call a recursive call.

Q1.3: No, because each function that will get more parameters than 1, can just return another function that gets the next parameters.
If the function gets n parameters, we can transform it into one parameter function, and return function that gets n-1 parameters, and its body is the same as the original function, recursivly.
So for example of activation, if we have a function f that gets 3 parameters:
(f x1, x2, x3),we can just transform it to: (((f x1) x2) x3).

Q1.4: No. because function the gets a function as argument, and applying it inside its body, cannot copy this behavior outside.
If we have function f(g(x)) we can't get g(x) before we got into f.


