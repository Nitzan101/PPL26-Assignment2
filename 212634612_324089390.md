Q1.1: No, Because in every program in L1 that uses 'define x', we can swich each 'x' with it's defenision.
Q1.2: Yes. Contradictory example:
    (define f
        (lambda (x)
            (if (< x 10) x (f (/ x 10)))
        )
    )
    (f 123)
    without 'define' we can't call a recursive call.
Q1.3: Yes. Contradictory example:
    (define f
        (lambda (x y)
            (if (> (+ x y) 100) 
                (* x y)
                (f (+ x 1) (+ y 2)))
        )
    )
    (f 12 56)
    Because of the recursive call we can't 'copy' the value in each apearness of the argument.
Q1.4: 
